[Unit]
Description=Nunux Reader Server
After=docker.service
Requires=docker.service redis.service

[Service]
ExecStartPre=-/usr/bin/docker kill reader-server
ExecStartPre=-/usr/bin/docker rm reader-server
ExecStart=/usr/bin/docker run --rm --name reader-server --env-file /media/data/reader/env.conf --link redis:db -P ncarlier/reader
ExecStop=/usr/bin/docker stop reader-server

[X-Fleet]
X-Conflicts=reader-server@*.service
