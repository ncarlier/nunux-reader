#!/bin/bash

###
# Simple script to get a shell into the container.
###
docker run \
    --rm \
    --name="reader" \
    --link redis:db \
    --env-file="./etc/reader.conf" \
    -i -t \
    --entrypoint="/bin/bash" \
    ncarlier/reader \
    -c /bin/bash

